---@diagnostic disable-next-line: lowercase-global
function main()
	return {
		debug_enabled = true,
		fps = "140", -- Default auto, set as auto or a number for specific frame rate target
		height = 800,
		width = 1000,
	}
end
